# Docker Compose override for CI - no volume mounts
# Usage: docker compose -f docker-compose.yml -f docker-compose.ci.yml up

services:
  app:
    user: "0:0"  # run as root 
    volumes: []  # Override to remove volume mount from base compose file
